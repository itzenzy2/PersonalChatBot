{
  "name": "gemini-chatbot-live",
  "version": "1.0.0",
  "description": "Gemini AI Chatbot with GitHub Models support",
  "dependencies": {
    "@google/generative-ai": "^0.2.1",
    "node-fetch": "^2.6.7"
  },
  "scripts": {
    "build": "echo 'No build step needed'"
  }
}
