[build]
  publish = "/"
  functions = "netlify/functions"

[functions]
  directory = "netlify/functions"
  # This explicitly tells Netlify to recognize Python functions
  included_files = ["netlify/functions/requirements.txt"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
